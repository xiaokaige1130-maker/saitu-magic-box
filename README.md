# 晒图魔方

本地图片整理工具，用于摄影/电商图片分类、重复识别、AI 初筛和人工复核。

## 功能

- 扫描本地图片文件夹，支持 jpg、png、bmp、webp、tif 等常见格式
- 识别完全重复、文件名副本、视觉相似图片
- 自动生成缩略图、质量分、清晰度、曝光、反差、饱和度指标
- 自动分类为“精选、较差、空镜、废片”，并生成问题标签
- 支持 10%、20%、50%、80% 推荐选优
- 支持卡片内快速改分类，键盘 1-5 设置星级
- 支持复制精选、废片、重复候选、组内最佳到输出文件夹
- 支持按分类整理、导出 CSV、生成 XMP 星级/标签侧车文件
- 默认不删除、不移动原图

## 快捷键

先点击一张图片卡片选中它：

- `1` 到 `5`：设置星级
- `F`：标为精选
- `W`：标为较差
- `E`：标为空镜
- `D`：标为废片

## 启动

```powershell
cd C:\Users\云电脑\image-cube
python -m uvicorn app.main:app --host 127.0.0.1 --port 18130
```

打开：

```text
http://127.0.0.1:18130
```

## 默认扫描目录

```text
C:\Users\云电脑\Desktop\白底图\外贸绞肉机图
```

## 打包 EXE

```powershell
cd C:\Users\云电脑\image-cube
.\build-exe.ps1
```

生成位置：

```text
C:\Users\云电脑\image-cube\dist\晒图魔方\晒图魔方.exe
```

## RAW 文件

代码已经识别 cr2、cr3、nef、arw、raf、dng 扩展名。当前默认依赖不强制安装 `rawpy`，因此没有 RAW 解码环境时这些文件会显示在扫描失败列表里；安装可用的 `rawpy` 后会自动读取 RAW 预览。

## 原则

- 不删除原图
- 不移动原图
- 只生成本地数据库、缩略图和筛选报告
